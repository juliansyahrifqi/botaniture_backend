export class CreateServiceDTO {
  service_name: string;
  service_desc: string;
  service_icon: string;
}

export class UpdateServiceDTO {
  service_name: string;
  service_desc: string;
  service_icon: string;
}